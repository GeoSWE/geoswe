# API reference

Auto-generated from the package docstrings. The most-used classes are
{py:class}`~geoswe.Mesh2D`, {py:class}`~geoswe.Config`, and
{py:class}`~geoswe.Solver2D`.

The public names are re-exported at the top level (e.g. `geoswe.Solver2D` is
`geoswe.solver.Solver2D`); the pages below document each module.

```{toctree}
:maxdepth: 2

solver
mesh
forcing
compressed
runlib
```
